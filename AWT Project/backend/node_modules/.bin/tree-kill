#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<<< HEAD:AWT Project/backend/node_modules/.bin/semver
  exec "$basedir/node"  "$basedir/../semver/bin/semver.js" "$@"
else 
  exec node  "$basedir/../semver/bin/semver.js" "$@"
========
  exec "$basedir/node"  "$basedir/../tree-kill/cli.js" "$@"
else 
  exec node  "$basedir/../tree-kill/cli.js" "$@"
>>>>>>>> f7def9b7127b4ca3cfcacc0a05c61177e2e37ed9:AWT Project/backend/node_modules/.bin/tree-kill
fi
